// 各服务 OpenAPI 自动生成的类型聚合入口。
// 使用：
//   import type { Auth } from '@/api/schema'
//   type Login = Auth.components['schemas']['LoginResult']
//
// 这些 namespace 下的 schema 由 `pnpm sync-api` 生成，本文件本身不会被覆盖。

export type * as Auth from './auth'
export type * as User from './user'
export type * as Product from './product'
export type * as Order from './order'
export type * as Message from './message'
